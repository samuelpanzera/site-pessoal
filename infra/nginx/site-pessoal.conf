# ==============================================================================
# CONFIGURAÇÃO NGINX: site-pessoal
# Mapeado sob domínio samuelpanzera.com.br para VPS Debian 13 (Multi-Tenant)
# ==============================================================================

# --- FRONT-END: samuelpanzera.com.br & www ---
server {
    listen 80;
    server_name samuelpanzera.com.br www.samuelpanzera.com.br;
    return 301 https://samuelpanzera.com.br$request_uri;
}

server {
    listen 443 ssl;
    http2 on;
    server_name samuelpanzera.com.br www.samuelpanzera.com.br;

    # Certificados SSL gerenciados pelo acme.sh
    ssl_certificate     /root/.acme.sh/samuelpanzera.com.br_ecc/samuelpanzera.com.br.cer;
    ssl_certificate_key /root/.acme.sh/samuelpanzera.com.br_ecc/samuelpanzera.com.br.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;
    ssl_session_cache   shared:SSL:10m;
    ssl_session_timeout 1d;

    # Redireciona www -> sem www
    if ($host = www.samuelpanzera.com.br) {
        return 301 https://samuelpanzera.com.br$request_uri;
    }

    # Cabeçalhos de segurança básicos
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options           "SAMEORIGIN"                          always;
    add_header X-Content-Type-Options    "nosniff"                             always;
    add_header Referrer-Policy           "strict-origin-when-cross-origin"     always;

    location / {
        proxy_pass         http://127.0.0.1:3100;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_connect_timeout 10s;
        proxy_read_timeout    30s;
    }
}

# --- API GO: api.samuelpanzera.com.br ---
server {
    listen 80;
    server_name api.samuelpanzera.com.br;
    return 301 https://api.samuelpanzera.com.br$request_uri;
}

server {
    listen 443 ssl;
    http2 on;
    server_name api.samuelpanzera.com.br;

    # Certificados SSL gerenciados pelo acme.sh
    ssl_certificate     /root/.acme.sh/samuelpanzera.com.br_ecc/samuelpanzera.com.br.cer;
    ssl_certificate_key /root/.acme.sh/samuelpanzera.com.br_ecc/samuelpanzera.com.br.key;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    # Cabeçalhos de segurança adicionais para API
    add_header Strict-Transport-Security "max-age=31536000" always;
    add_header X-Content-Type-Options    "nosniff"          always;

    location / {
        proxy_pass         http://127.0.0.1:3101;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_connect_timeout 10s;
        proxy_read_timeout    30s;
    }
}
